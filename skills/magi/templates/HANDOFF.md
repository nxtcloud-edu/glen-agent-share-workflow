# HANDOFF — {{PROJECT}}

> 다음 에이전트가 이어받을 때 먼저 볼 내용. 카스파 단일 관리.

## 노드 공통 시작 절차

`CURRENT_STATE.md` 읽기 → 이 파일 읽기 → `git status --short --branch` 실측 대조 →
자기 브랜치 확인(멜기오르 `wo/*`, 발타자르 `review/*`) → 작업.

**세션 기동**: 빈 터미널이면 `magi`. 이미 tmux 안이면 `magi status`로 3노드 생존을 먼저 확인한다.
